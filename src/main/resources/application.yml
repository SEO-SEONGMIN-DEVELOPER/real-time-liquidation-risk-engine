spring:
  profiles:
    active: local

  application:
    name: risk-engine

# Binance Futures WebSocket 설정
binance:
  ws-base-url: wss://fstream.binance.com
  rest-base-url: https://fapi.binance.com
  symbols:
    - btcusdt
    - ethusdt
  streams:
    - markPrice
    - forceOrder
    - depth@500ms
  mark-price-speed: 1000
  depth-level: "20"
  reconnect-interval-ms: 5000
  max-reconnect-attempts: 0
  open-interest-poll-interval-ms: 3000

# Cascade Risk 스로틀 설정
risk:
  cascade:
    throttle-ms: 200

# Monte Carlo 시뮬레이션 설정
montecarlo:
  enabled: true
  path-count: 10000
  time-step-minutes: 1
  horizons:
    - 10
    - 60
    - 240
    - 1440
  volatility-window: "1h"
  throttle-interval-seconds: 60
  use-fat-tail: false
  degrees-of-freedom: 5.0

---
# Local Profile
spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:h2:mem:riskengine;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        default_batch_fetch_size: 100
    open-in-view: false

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

logging:
  level:
    com.liquidation.riskengine: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

---
# Dev Profile
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/riskengine
    username: riskengine
    password: riskengine
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

logging:
  level:
    com.liquidation.riskengine: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

---
# Local Test Profile
spring:
  config:
    activate:
      on-profile: light

binance:
  symbols:
    - btcusdt
  mark-price-speed: 3000
  open-interest-poll-interval-ms: 15000

risk:
  cascade:
    throttle-ms: 2000

montecarlo:
  path-count: 1000
  time-step-minutes: 5
  throttle-interval-seconds: 180

logging:
  level:
    com.liquidation.riskengine: INFO
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN

---
# Production Profile
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_batch_fetch_size: 100
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    com.liquidation.riskengine: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: never
